# Legacy static prototype

Early HTML/CSS prototype before the React + FastAPI application.

**Do not use for viva demo** — use the React app via `START_NEUROSCAN.bat` instead.

Open `index.html` in a browser only for historical reference.
